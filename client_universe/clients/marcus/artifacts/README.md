# Client Artifacts

Client-facing artifacts for `client_id: marcus` belong here when they are stored as repo-visible fixtures or examples.

Runtime-created artifacts should be tracked by the artifact service or database and linked back to `client_id`.
