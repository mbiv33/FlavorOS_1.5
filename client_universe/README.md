# Client Universe

Client-scoped state model — see [docs/client_universe_model.md](../docs/client_universe_model.md).

- `schemas/` — JSON Schema fragments for MVP slices (start with profile).
- Other folders reserve future normalized storage layouts.
